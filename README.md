# wasm-game-of-life  

working version of [the webassembly tutorial](https://rustwasm.github.io/docs/book/)

to run:

``wasm-pack build``

go to ``www`` folder and run:

```
pnpm i
pnpm run start
```

open ``localhost:8080`` in your browser
